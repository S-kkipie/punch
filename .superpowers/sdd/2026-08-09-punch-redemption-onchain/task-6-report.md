# Task 6 report

## Status
DONE

## Implemented
- Wired `PunchVault.setRedeemer` in `scripts/dev-chain.ts` to the configured relayer wallet and awaited the receipt.
- Changed `requestPunchRedemptionService` to use `getConsumerBalance`; null/stale balances now return the existing 422 balance error.
- Extended bootstrap product discovery and chain wiring to include approved active reward products. Emissions retain IDs in creation order; rewards are numbered after emissions and use `ProductKind.Reward` (1).
- Added idempotent `ensureEligibleProducts` for mapped/recovered cafés so rerunning bootstrap repairs missing reward eligibility without sending `NoStateChange` writes.
- Added unit coverage for reward numbering/kind and stale balance refusal.
- Added the gated live redemption journey covering redeemer wiring, chain burn/payout, indexer projection, and double-approve dedupe.

## Clean sequence
Credentials were supplied through shell variables parsed from `.env`; no credentials were printed or written:
1. Dropped and recreated `punch_live_t6`.
2. `DATABASE_URL='<redacted isolated database>' DATABASE_SSL=false pnpm db:migrate`
3. `DATABASE_URL='<redacted isolated database>' DATABASE_SSL=false pnpm db:seed`
4. Started isolated Anvil on port 8546 and recorded its PID.
5. `CHAIN_RPC_URL=http://127.0.0.1:8546 pnpm chain:deploy`
6. `DATABASE_URL='<redacted isolated database>' DATABASE_SSL=false CHAIN_RPC_URL=http://127.0.0.1:8546 pnpm chain:bootstrap-local`
7. `DATABASE_URL='<redacted isolated database>' DATABASE_SSL=false CHAIN_RPC_URL=http://127.0.0.1:8546 pnpm chain:seed-history`
8. `DATABASE_URL='<redacted isolated database>' DATABASE_SSL=false CHAIN_RPC_URL=http://127.0.0.1:8546 CONSUMER_CHAIN_MODE=local PUNCH_RUN_INTEGRATION=1 PUNCH_RUN_LIVE_CHAIN=1 pnpm vitest run src/core/chain/server/__tests__/redemption-journey.live.test.ts --no-file-parallelism`

The isolated Anvil process was stopped by its recorded PID. The unrelated node on port 8545 was not touched.

## TDD evidence
- RED: before the deployment wiring, the live redeemer assertion observed the unset/zero redeemer and the redemption path was expected to hit `NotRedeemer`. After wiring, the redeemer assertion passed.
- The first live run then exposed two real earlier-task gaps: the request service read the mock-era balance table, and bootstrap omitted reward products. These were fixed as part of the Task 6 addendum rather than worked around in the test.
- GREEN: the final clean live run passed all 3 tests. It proved the redeemer equals the relayer wallet, the consumer vault balance decreased by exactly 12 PUNCH, the café owner's MockPEN balance increased by exactly 3,600,000 units, the request became `confirmed`, and the café payout projection reached 360 centimos.
- The double-approve test re-approved the already confirmed request, ran the relayer twice, and asserted unchanged chain balances plus exactly one `relayer_job`. It would fail if the ledger dedupe guard were removed because the second on-chain redemption would burn another 12 and pay another 3,600,000.

## Verification
- `pnpm vitest run --no-file-parallelism`: 532 passed, 50 skipped.
- Final gated live test: 3 passed.
- `pnpm typecheck`: passed.
- Biome check on all touched files: passed.

## Files changed
- `scripts/dev-chain.ts`
- `scripts/bootstrap-local.ts`
- `src/core/chain/server/bootstrap-local/service.ts`
- `src/core/chain/server/bootstrap-local/repository.ts`
- `src/core/chain/server/bootstrap-local/__tests__/service.test.ts`
- `src/core/chain/server/__tests__/redemption-journey.live.test.ts`
- `src/core/consumption/server/services/request-punch-redemption-service.ts`
- `src/core/consumption/server/services/__tests__/request-punch-redemption-service.test.ts`

`src/core/chain/addresses.local.json` was regenerated by deployment and was not staged.

## Concerns
- `chain:seed-history --seed-history` remains a distinct setup step in the repository even though the task brief describes bootstrap as supplying the historical 11; it was included explicitly in the reproducible clean live sequence because the existing historical seeder owns those receipts.
